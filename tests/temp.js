import { test, expect } from 'vitest'

// The auth and crud test files have been moved to ./tests-disabled/,
// because they require a DB to run. And we don't have that setup.

test('we dont have tests', async () => {
	expect(1).toBe(1)
})

